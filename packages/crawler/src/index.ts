/**
 * Shared crawler logic: source adapters (GitHub, npm, PyPI, awesome-lists),
 * README parsing, and repository URL canonicalisation.
 *
 * Consumed by the GitHub Actions workers in `workers/`.
 *
 * Implemented in Phase 2.
 */

export {};
